# AI Visual Acceptance

Open `index.html` directly in a modern browser, or serve this folder with any static HTTP server.

This deliverable is a self-contained single-file React application; it does not require installing dependencies or running a build step.
